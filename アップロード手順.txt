芝生RGB簡易診断 Ver0.6.3.2 レポート表示統一版

【修正内容】
・画面で選択した表示方式を、相談用レポートにもそのまま反映
・数値グラデーション選択時は、レポートにも同じグラデーション画像を掲載
・レポートに対象指数名（総合活性スコア／VARI／GLI／ExG）を表示
・レポートに数値カラーバーを追加
・従来の低活性候補表示も引き続き選択可能

【GitHubへの反映】
1. golf-rgb-diagnosis リポジトリを開く
2. Add file → Upload files
3. このZIP内の app.js と sw.js をアップロードして上書き
4. Commit changes を押す
5. Deployments の github-pages が緑になるまで待つ
6. アプリを開き直して確認

【重要】
・以前に保存した診断画像や相談用レポートは変更されません。
・診断数値、判定、教師データはそのままです。
・今回変わるのは、今後作成する相談用レポートの表示画像だけです。
